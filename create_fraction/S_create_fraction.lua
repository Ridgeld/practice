function CreateFraction( fraction_id, lider_name, fraction_players )
    FRACTION = {
        fraction_id = fraction_id,
        lider_name = lider_name,
        fraction_players = fraction_players
    }
end
